# Open Problems — Ordered by Leverage

## P0 — Find a materially new causal directional source

Current H12 directional core is non-stationary: fold 1 reverses sign and LONG loses broadly. The next useful work is **not another H12 threshold**.

Smallest question:
> Is there already an unused, causal, pre-decision variable or existing repo factor that explains when the sign of expected H12 return changes across time, with the same direction of effect across temporal development slices?

Acceptance before any performance trial:
- available causally at decision time;
- economically interpretable;
- distinct from H1-H8/V10 rejected fingerprints;
- stable across at least two chronological development folds;
- positive aggregate development economics vs baseline;
- does not depend on fold identity;
- not selected by inspecting prospective/untouched outcomes.

## P1 — Establish enough untouched post-boundary evidence

V11 prospective boundary: `2026-08-23T17:16:03.853710+00:00`.
Earliest complete 12h label maturity was `2026-08-24T05:16:03.853710+00:00`; maturity alone does not prove sufficient sample size or eligibility.

Need to prove:
- recorder continuity and provenance;
- exact decision timestamps and causal feature availability;
- no inspection of outcomes during candidate design;
- enough temporal/symbol coverage to justify one held-out evaluation.

Until proven: `NEED_MORE_UNTOUCHED_EVIDENCE`; do not consume 871.

## P2 — Separate sign instability from cross-sectional ranking quality

H12 may fail as a directional absolute-return predictor while retaining relative ranking information.

Question:
> Does causal cross-sectional rank of H12 still order future returns monotonically even when aggregate LONG direction is wrong?

This is diagnosis first, not permission to build a long-short candidate. Use observed/development data only.

## P3 — Explain fold-1 regime shift with low-dimensional causal statistics

Do not search many thresholds. Compare distributions and effect signs for a compact set already available before decision time:
- trend/activity/volatility states;
- funding state;
- cross-sectional dispersion;
- H12 score/rank magnitude;
- portfolio state and concentration;
- session;
- existing positioning/L2 only when timestamp provenance makes them causal for the evaluated period.

Goal: identify a repeatable mechanism, not label fold 1.

## P4 — Multiple-testing provenance

DSR/PBO/CSCV/CPCV remain `NOT_COMPUTABLE_WITH_CURRENT_PROVENANCE` for a promoted V11 candidate. Build the aligned configuration/return ledger **only if** future selection actually creates enough candidate paths. Do not add statistics framework preemptively.

## P5 — Paper readiness

Blocked by SCIENCE and FREEZE. Existing execution/paper infrastructure should be reused, not rewritten. Once and only once a candidate passes held-out + robustness + freeze gates, transition to A1 paper simulation.
