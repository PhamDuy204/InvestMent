# Open Problems — Ordered by Leverage

## P0 — Find a materially new causal source outside H12 rank/direction rescue

V12 falsified the simplest relative-edge rescue: H12 cross-sectional rank is not stable across chronological slices. Do not convert H12 into a rank-only long/short mechanism.

Smallest next question:
> Does an already-existing, causal pre-decision variable in the repo explain H12 sign failure consistently across at least two chronological development slices, without using fold identity or retuning prior rejected filters?

Acceptance before any performance trial:
- causal at decision time; economically interpretable; distinct from H1-H8/V10 rejected fingerprints; same interaction direction in >=2 chronological development slices; positive aggregate development economics; no prospective outcome inspection.

## P1 — Establish enough untouched post-boundary evidence

Boundary remains `2026-08-23T17:16:03.853710+00:00`. Recorder continuity is healthy at this checkpoint, but sufficient matured H12 sample size/provenance has not been established. Until proven: `NEED_MORE_UNTOUCHED_EVIDENCE`; do not consume 871.

## P2 — Explain fold-1 regime shift with one low-dimensional causal interaction

Inspect only compact existing causal variables before inventing anything: trend/activity/volatility, funding state, cross-sectional dispersion, portfolio state/concentration, session, and any existing positioning/L2 feature only where timestamp provenance permits. Do not threshold-search.

## P3 — Runtime/environment reproducibility

The only discovered reusable venv (`InvestMent-v9-paper-readiness/.venv`) is stale relative to V11 source: importing V11 fails because its installed `ReliabilityGateConfig` lacks `flat_trend_scale`. Do not use that venv for V11 scientific reruns. Reuse a verified environment if one exists; otherwise minimally reconcile the environment before a code-changing scientific run.

## P4 — Multiple-testing provenance

DSR/PBO/CSCV/CPCV remain `NOT_COMPUTABLE_WITH_CURRENT_PROVENANCE`. Build aligned strategy-return provenance only if future selection creates enough genuine candidate paths.

## P5 — Paper readiness

Still blocked by SCIENCE and FREEZE. Existing execution/paper infrastructure should be reused, not rewritten.
