# Root Cause Split — V30

The current blocker is now **evidence availability**, not missing L2 code.

The recorder already computes the simplest microstructure feature and its stored snapshots are internally consistent at the metadata/checksum/cadence level. The problem is that only ~6 hours fall inside the fixed DEVELOPMENT boundary, while later observations are reserved and separated by a large outage.

Thus the next useful work is provenance/boundary acquisition, not another model or heuristic.
