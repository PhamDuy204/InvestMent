# Questions — V30

1. Is there an auditable pre-cutoff historical top-of-book/L2 source with stable semantics and enough coverage to create genuinely chronological H12 development slices?
2. If not, what future timestamp can be fixed prospectively as a new development/untouched boundary before any corresponding outcomes are opened?
3. How much continuous L2 is required so H12 labels are not dominated by overlap and one market regime?
4. Can the existing `top_of_book_imbalance` show stable sign/order across non-overlapping chronological slices before any portfolio mapping?
