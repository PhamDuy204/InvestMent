# Open Problems — V23

## P0 — Test H21-C exactly once

Feature: `sign(ret_4) * oi_log_change_1h`.

Same fixed gate: mean timestamp IC >0 in >=2/3 folds, top2-bottom2 mean spread >0 in >=2/3 folds, aggregate mean IC >0.

If this fails, retire the bounded historical Binance metrics ladder rather than inventing a fourth metric transform.

## P1 — Next factor family if C fails

Return to genuinely new information families (forward L2, other auditable historical microstructure, or macro/event variables) rather than expanding price-only/metrics model complexity.
