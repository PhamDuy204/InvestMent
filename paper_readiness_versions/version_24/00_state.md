# State — Version 24

V24 records the first materially new historical mechanism to pass its predeclared predictive stability gate.

- H21-C `oi_trend_confirmation = sign(ret_4) * oi_log_change_1h` was the third and final rung of the V21 ladder.
- It passes without threshold/model fitting: aggregate mean timestamp IC `+0.017813`; mean IC positive in `2/3` folds; top2-bottom2 mean H12-return spread positive in `3/3` folds.
- Fold ICs: `+0.023921`, `-0.000817`, `+0.030336`.
- Fold spreads: `+0.001376`, `+0.000760`, `+0.001453`.
- H21-A and H21-B remain rejected and are not retuned.
- Per V21, the mechanism ladder stops now; no fourth metrics transform is allowed.
- H21-C is not yet a candidate/freeze. Next step is its predeclared raw score through the exact corrected V11 economic replay.
- No post-2026-08-20 outcome was used. Trial 871 remains untouched. `LIVE_NOT_AUTHORIZED`.
