# Minimal Plan Toward Paper Readiness — V16

1. Keep the current L2 recorder running; do not restart it without operational need.
2. Inspect only L2 schema/timestamps/counts next. Reuse `src/crypto_research/l2_shadow_v8.py` fields and helpers.
3. Predeclare a new forward research boundary and a development/untouched split from time coverage alone, including a 12h purge gap if H12 remains the target horizon.
4. Pick one parameter-light imbalance statistic already derivable from recorded fields; no threshold grid.
5. Run chronological development diagnostics only after the development block has sufficient coverage.
6. If development fails, retire the L2 mechanism; do not rescue parameters.
7. If it passes, then reconcile runtime, freeze exact candidate/evaluation methodology, audit provenance and seek council authorization before any new performance trial.
8. Start paper simulation only after all readiness categories pass. Never authorize live/private trading.
