# Research Questions — V16

1. What exact fields and cadence does the current `public_l2` recorder provide per symbol?
2. How much contiguous time coverage and how many independent timestamps exist now without reading outcomes?
3. Can one imbalance statistic be defined directly from existing bid/ask depth fields with no fitted parameter?
4. What prospective boundary rule can be frozen using only timestamps/counts?
5. How should development and untouched blocks be separated given a 12h label horizon so labels cannot overlap the boundary?
6. What minimum cross-symbol coverage is required to avoid a single-symbol microstructure result?
7. What result would retire the new L2 family rather than trigger another filter search?
